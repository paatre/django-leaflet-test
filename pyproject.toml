[project]
name = "django-leaflet-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django-leaflet>=0.31.0",
]

[dependency-groups]
dev = [
    "ruff>=0.11.2",
]
